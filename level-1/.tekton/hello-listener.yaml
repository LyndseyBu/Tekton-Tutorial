apiVersion: tekton.dev/v1beta1
kind: EventListener
metadata:
  name: eventlistener
  namespace: default
spec:
  triggers:
    - binding:
        name: hello-trigger-binding
      template:
        name: hello-trigger-template
