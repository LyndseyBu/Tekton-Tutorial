apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: hello-listener
  namespace: default
spec:
  triggers:
    - bindings:
        name: hello-trigger-binding
      template:
        name: hello-trigger-template
