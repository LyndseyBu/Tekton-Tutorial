apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: hello-task
spec:
  steps:
    - name: sleep2mins
      image: ubuntu
      env:
      - name: PIPELINE_NAME
        value: $(params.pipeline-name)
    script: |
      echo "Pipeline name is $PIPELINE_NAME"
